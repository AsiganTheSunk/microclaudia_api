[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers -m "not integration"
markers =
    integration: live MicroClaudia API tests (network + credentials required)
    live_all_methods: full live probe of each MicroClaudiaAPI method
filterwarnings =
    ignore::DeprecationWarning
